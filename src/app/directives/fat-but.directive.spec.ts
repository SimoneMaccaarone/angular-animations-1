import { FatButDirective } from './fat-but.directive';

describe('FatButDirective', () => {
  it('should create an instance', () => {
    const directive = new FatButDirective();
    expect(directive).toBeTruthy();
  });
});
